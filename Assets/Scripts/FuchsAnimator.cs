using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FuchsAnimator : MonoBehaviour
{
/*
    public Animator animator;
    public GameObject Player;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        Player player = Player.GetComponent<Player>();

        Vector3 inputVector = (Vector3.forward * Input.GetAxis("Vertical")) + (Vector3.right * Input.GetAxis("Horizontal"));

        Vector3 animationVector = player.transform.InverseTransformDirection(inputVector);

        var VelocityX = animationVector.x;
        var VelocityZ = animationVector.z;
 
        animator.SetFloat("x", VelocityX);
        animator.SetFloat("z", VelocityZ);*/
        
    }

